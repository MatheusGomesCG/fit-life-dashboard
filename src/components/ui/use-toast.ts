
// This is a re-export file that points to the real implementation

import { useToast, toast } from "@/hooks/use-toast";

// Re-export the toast functions to maintain compatibility with imports
export { useToast, toast };
